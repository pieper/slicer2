package require vtk

#
# rely on the fact that a class loaded from the shared
# library is uniquely available through this module
#

if {[info commands vtk@Modulename@] != "" ||
    [::vtk::load_component vtk@Modulename@TCL] == ""} {
    package provide vtk@Modulename@ 1.0
}
